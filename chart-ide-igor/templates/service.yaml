apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-ide-igor.fullname" . }}
  labels:
    {{- include "chart-ide-igor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-ide-igor.selectorLabels" . | nindent 4 }}
